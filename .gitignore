# Large video files — exceed GitHub's 100MB file limit, hosted externally instead
*.mp4
*.mov
*.avi
*.mkv

# Local tooling / editor config
.claude/

# Node dependencies (used only for local image optimization)
node_modules/
